services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_MAPBOX_TOKEN: ${VITE_MAPBOX_TOKEN:-}
        VITE_API_URL: ""
        VITE_WS_URL: ""
    ports:
      - "3000:80"
    depends_on:
      - backend
